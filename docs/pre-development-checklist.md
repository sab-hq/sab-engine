# Pre-Development Checklist

### `sab-engine` — What's Left Before Writing Code

**Status:** Living document — update as items move, and add new items as they surface. Not a design document; everything here assumes the design decisions in `SAB_Design_Document_v0.1.2.md` and `open-questions.md` are already settled.

**Ordering:** Items are listed in the order they actually need to happen — each one generally depends on the ones above it. IDs are assigned once and don't get renumbered as items complete; if the order needs to change later, move the row and add a note rather than reassigning IDs.

**Status key:**
- ⬜ **Not Started**
- 🟡 **In Progress**
- 🔒 **Blocked** — waiting on something else, noted in the item
- ✅ **Done**

---

| ID | Item | Why it's here / depends on | Status |
|---|---|---|---|
| PD-1 | Decide: does `sab-engine` build work start now, or does `sab-kb` still take priority per the Section 9 sequencing note (`open-questions.md` RC-5)? | Nothing below can meaningfully start until this is answered | ✅ Done — **both, in parallel, confirmed by Brock.** Not strict sequencing after all; this actually matches RC-5's own resolution ("'both' is the actual working mode, not strict sequencing") rather than overriding it. PD-2 onward can proceed. |
| PD-2 | Scaffold the `sab-engine` C#/.NET solution structure (per TS-1, confirmed) | The base project everything else gets built inside | ✅ Done — **verified locally by Brock:** `dotnet build` succeeds (0 warnings, 0 errors), `dotnet test` passes (1/1). Solution, 6 projects, and 1 test project written (`SabEngine.sln`; `src/SabEngine.{Core,Data,Orchestration,Agent,Execution,Api}`; `tests/SabEngine.Core.Tests`). `SabEngine.Core` has real domain types matching Section 4.1's data model (`WorkflowState`, `WorkflowRun`, `Plan`, `Approval`, `ExecutionResult`, `AuditEntry`) plus the module and connector contracts (`IModuleContract`, `IExecutionConnector`) from Sections 4.2/4.4. Dependency direction follows the design doc's layering (`Agent` and `Execution` only depend on `Core`, never on each other, per Section 4.3's hard boundary). Everything else (state machine logic, EF Core/Postgres wiring, Semantic Kernel, WinRM) is intentionally still a stub — that's PD-3 onward. |
| PD-3 | Stand up PostgreSQL and implement the Section 4.1/4.5 schema — `workflow_runs`, `plans`, `approvals`, `execution_results`, `target_state`, `notes`, `AuditEntry` (per TS-3, confirmed) | The orchestration engine is stateless by design (AR-1) — this has to exist before the engine has anywhere to persist state | ✅ Done — **verified locally by Brock:** `dotnet ef migrations add InitialCreate` generated cleanly, `dotnet ef database update` applied it with no errors. All 7 tables exist in Postgres with the exact snake_case names from Section 4.5. Fixed two bugs found during verification: (1) a missing `using Microsoft.Extensions.Configuration;` in `Program.cs`; (2) `Microsoft.EntityFrameworkCore.Design` needed on the *startup* project (`Api`), not just the project with the `DbContext` (`Data`). Also fixed two non-blocking EF Core warnings by adding proper `ValueComparer`s for the `jsonb`-backed `Plan.Steps` and `TargetState.Facts` properties. **Real infrastructure gotcha, not a code bug:** Brock's machine has a native PostgreSQL 15 Windows service already bound to port 5432, which was silently intercepting every connection meant for the Docker container (same port, two unrelated databases — diagnosed via `docker ps` + `netstat -ano` showing two different PIDs on 5432). Fixed by moving the Docker container to host port 5433 in `docker-compose.yml`, `appsettings.json`, and `SabEngineDbContextFactory.cs`. |
| PD-4 | Implement the orchestration engine's state machine (`Requested → PlanDrafted → PendingApproval → Approved/Declined → Executing → Completed/Failed → RolledBack`, Section 4.1) | Depends on PD-2/PD-3 existing to persist against | ✅ Done — **verified locally by Brock:** `dotnet build` clean, `dotnet test` passes 6/6 (1 from `SabEngine.Core.Tests`, 5 from the new `SabEngine.Orchestration.Tests`). `WorkflowRunStateMachine` enforces the exact allowed-transitions map from Section 4.1 in code (an illegal transition throws `InvalidWorkflowStateTransitionException`) — also allows `Declined → PlanDrafted`, matching Section 4.3's "the agent can revise and try again" on the same run. The `OrchestrationEngine` class from PD-2 is still a stub — wiring the state machine to actually call modules via `IExecutionConnector` during `Executing` isn't itemized as its own PD- item yet. |
| PD-5 | Implement the claim/lease concurrency pattern for stateless workers (Section 4.1's concurrency model) | Extends the state machine (PD-4) to support more than one worker safely | ✅ Done — **verified locally by Brock:** `dotnet build` clean, `dotnet test` passes 20/20 (1 from `SabEngine.Core.Tests`, 19 from `SabEngine.Orchestration.Tests` — 5s runtime, confirming the 7 claim-service tests genuinely exercised real Postgres rather than silently skipping). `WorkflowRunClaimService.TryClaimNextAsync` atomically claims the oldest eligible run via `ExecuteUpdateAsync`; claimable states are `Requested`, `Declined`, `Approved`, `Failed`. Required switching this test class from InMemory to a real, disposable-per-test Postgres database (`PostgresTestDatabase.cs`) after InMemory proved unable to translate `ExecuteUpdateAsync` — documented above. **New standing prerequisite: Docker must be running (`docker compose up -d`) for `dotnet test` to fully pass going forward**, not just for PD-3's migration step. |
| PD-6 | Integrate Microsoft Semantic Kernel for the AI agent layer (per TS-2, confirmed) | Needs the solution scaffold (PD-2) in place first | ✅ Done — **verified locally by Brock:** `dotnet build` clean, `dotnet test` passes 27/27 (1 from `SabEngine.Core.Tests`, 7 from the new `SabEngine.Agent.Tests`, 19 from `SabEngine.Orchestration.Tests`). One real bug caught and fixed: a missing `using Microsoft.Extensions.DependencyInjection;` in the test file (`AddSingleton` is an extension method). `SabAgent` builds a prompt from a workflow/target/available-module list, calls the Kernel's `IChatCompletionService`, and parses the response into a real `Plan`. **Section 4.1/4.2's hard rule is enforced here too, as defense in depth** — `ValidateAgainstHardRule` rejects any plan that references an unknown module, one that isn't `production-approved`, or one without a tested rollback, throwing `PlanValidationException` rather than silently filtering; covered by 5 of the 7 new tests. **Real, deliberate scope limit:** there's no module catalog loader yet (no real modules exist — PD-14–PD-17 aren't done), so `ProposePlanAsync` takes the available-module list as a parameter rather than loading one itself; a new `ModuleCandidate` type in `SabEngine.Core` makes that list constructible. **Also deliberately not done: wiring to a real model.** Only the core `Microsoft.SemanticKernel` package is referenced (version 1.26.0, confirmed correct via successful restore) — no OpenAI/Azure OpenAI connector, no API key. That needs a real credential Brock provides, which doesn't belong hardcoded in this repo; genuinely open, not itemized as its own PD- yet. |
| PD-7 | Set up PowerShell interop via `System.Management.Automation` (native to the .NET/TS-1 choice) | Needs the solution scaffold (PD-2); required before any module can actually run PowerShell | ✅ Done — **verified locally by Brock:** `dotnet build` succeeds (with expected `NU1608` warnings, not errors), `dotnet test` passes 32/32 (1 + 7 + 5 + 19 across all four test projects). `PowerShellExecutor` runs a script locally via `Microsoft.PowerShell.SDK` and returns a structured `PowerShellExecutionResult`. Deliberately scoped to **local** execution only — the WinRM connector (PD-17–PD-20, not built yet) will reuse this same interop pointed at a remote session instead. **Two real issues found and fixed during verification:** (1) a `Microsoft.CodeAnalysis.Common`/`Microsoft.CodeAnalysis.CSharp` version conflict (`NU1107`) between `Microsoft.PowerShell.SDK` and `Microsoft.EntityFrameworkCore.Design`'s transitive dependencies, resolved by pinning both packages explicitly in `SabEngine.Api.csproj` to the newer version (4.9.2), exactly as NuGet's own error message suggested; (2) a genuine logic gap — PowerShell's `throw` produces a *terminating* error that `ps.Invoke()` propagates as a real .NET exception, unlike `Write-Error`'s non-terminating error (captured gracefully via `ps.Streams.Error`/`HadErrors`). The original code only handled the second case; a script-level `throw` would have crashed the caller with an unhandled exception instead of returning a structured failed result. Fixed with a try/catch around `Invoke()` that folds the exception message into the same `Errors` list. The `Microsoft.PowerShell.SDK` version guess (7.4.6) turned out correct. |
| PD-8 | Implement audit logging — every `AuditEntry` write tied to a real state transition, append-only/hash-linked per the tamper-evidence approach in Section 7 | Hooks directly into the state machine (PD-4) — cheapest to build alongside it, not bolted on after | ✅ Done — **verified alongside PD-4** (same test run, same hash-chain test). `WorkflowRunStateMachine.TransitionAsync` writes a hash-linked `AuditEntry` in the same operation as every state change — no separate audit-logging pass was needed. |
| PD-9 | Choose and stand up the Phase 1 secrets backend — HashiCorp Vault or native OS credential store, per SE-1 | Required before the execution environment (PD-16+) can resolve any real credential | ✅ Done — **verified locally by Brock:** `dotnet build` succeeds (warnings only, same expected `NU1608` category as PD-7), `dotnet test` passes 38/38 (1 + 7 + 11 + 19 across all four test projects) — the P/Invoke marshaling worked correctly on the first attempt, including the Unicode round-trip test (accented characters, CJK, and an emoji). Per SE-1's direction: implemented against **Windows Credential Manager** as the Phase 1 default, behind the pluggable `ISecretStore` contract in Core, so a Vault-backed implementation can swap in later without touching `IExecutionConnector` or anything that calls it. Handles are namespaced under a `SabEngine:` prefix in Credential Manager's target-name space. `[SupportedOSPlatform("windows")]` added proactively to avoid `CA1416` platform-compatibility warnings on the Windows-only P/Invoke calls. |
| PD-10 | Set up a CI pipeline for `sab-engine` (build + run tests on push) | Best set up now, before real code accumulates, so everything built from here on gets tested continuously | ✅ Done — **verified on GitHub Actions, both jobs green:** `test-cross-platform` (ubuntu-latest) succeeded in 1m 3s, `test-windows` (windows-latest) succeeded in 1m 31s. The two-job, split-by-OS design worked as intended on the very first push — `test-cross-platform` runs everything except the Windows-only Credential Manager tests against a real `postgres:16` service container; `test-windows` runs the full `SabEngine.Execution.Tests` project, including the Credential Manager tests, with no Postgres dependency. (One false alarm during verification: the live log view appeared stuck at 16+ minutes on `test-windows`, prompting an attempted cancel — GitHub reported "cannot cancel a check suite that is completed," confirming it was a stale browser view, not a real hang; the job had already finished in 1m 31s. Added `--blame-hang-timeout`/`--blame-hang-dump-type mini` on the Windows test step and `timeout-minutes: 15` on both jobs regardless, as free insurance against a genuine future hang producing real diagnostics instead of silence — not exercised this run, but worth keeping.) `PostgresTestDatabase.cs` now reads its admin connection string from an optional `SABENGINE_TEST_ADMIN_CONNECTIONSTRING` environment variable, which is what let CI point the claim-service tests at its own Postgres service container without any code changes. |
| PD-11 | Stand up a lab/low-stakes Windows Server environment to test against — required before anything touches production, per Section 2's reliability principle | Doesn't block earlier items, but needs to exist before any real module or connector testing (PD-19+) happens — worth starting in parallel with PD-2–PD-10 | ✅ Done — **VM created and confirmed reachable by Brock.** `sabengine-labwin01`, Windows Server 2022 Datacenter: Azure Edition Hotpatch (Gen2), Central US, resource group `SAB`. **Plan changed from B1s to D2s_v3 during setup** — B-series (including B1s, B1ms, B2s) returned `NotAvailableForSubscription` in both East US and Central US, a subscription-level quota gap rather than a regional one; D2s_v3 was the first general-purpose size that was actually available. Real cost impact: **$0.2110/hr** confirmed at deployment (~$154/month if left running continuously, vs. B1s's original ~$15–$20/month estimate) — makes the deallocate-between-sessions discipline meaningfully more important than originally planned. **Budget alert set** (Cost Management + Billing → Budgets, $10/month threshold, 80% trigger) before any further use, per the plan. Standard HDD disk (not Premium SSD), RDP confirmed reachable (`Test-NetConnection … -Port 3389` → `TcpTestSucceeded: True`), then stopped via **Stop (deallocated)** immediately after confirming connectivity — not left running. **One flagged tradeoff, not yet acted on:** the image is Hotpatch-enabled, which lets many Windows updates apply without a reboot — fine for early connectivity/module testing, but may not exercise sab-engine's actual reboot-handling path in `apply-patches`/`validate`/rollback the way a non-Hotpatch image would. Worth a second, non-Hotpatch VM later specifically for reboot-path testing, once that's the thing being tested — not itemized as its own PD- entry yet. WinRM (5985/5986) reachability — what PD-17–PD-20's actual connector will use — not yet tested; deferred until closer to building the connector itself. |
| PD-12 | Implement the module metadata manifest format/parser (per the Section 4.2 YAML schema) | Needed before any real module can be written and recognized by the engine | ✅ Done — **verified locally by Brock:** `dotnet build` succeeds with 0 warnings (a `NU1608` suppression was added to `SabEngine.Api.csproj`, documenting exactly why — same underlying PD-7 version pin as before, not a new issue), `dotnet test` passes 48/48 (1 + 10 + 7 + 11 + 19 across all five test projects). **A genuinely interesting bug found and fixed during verification, not in the parser's logic but in the design doc's own reference example:** `docs/design/SAB_Design_Document_v0.1.2.md` Section 4.2's literal YAML example contains `{ type: enum[success, failure] }` — inside a flow mapping, `[`, `]`, and `,` are structural characters, so this isn't actually valid YAML as written; a real module author copying it verbatim would hit a parse failure. Fixed by quoting the value (`{ type: "enum[success, failure]" }`) in both the design doc itself and the test fixture that mirrors it — caught specifically because the test suite used the design doc's own example as its base fixture rather than a simplified stand-in, which is exactly the value of testing against the real reference example instead of an easier one. `SabEngine.Modules` (new project, kept separate from `SabEngine.Core` since Core is dependency-free and this needs `YamlDotNet`) enforces every Section 4.2 required field at parse time, and `ToModuleCandidate()` closes the loop PD-6 left open — a real parsed manifest can now feed `SabAgent.ProposePlanAsync` directly. The `YamlDotNet` version guess (16.2.1) turned out correct. |
| PD-13 | Set up a CI pipeline for `sab-modules` (the OSML) that validates new/changed modules against the contract (unique ID, required fields, rollback present, tests present) | Depends on PD-12's manifest format existing to validate against | ✅ Done — **verified both locally and on GitHub Actions.** Locally: `dotnet run --project src/SabEngine.Modules.Cli -- ../sab-modules/modules` confirmed `pre-flight-check`'s manifest parses correctly (`1/1 manifest(s) valid`) — this also closed the exact verification gap PD-14 flagged as unresolved. On GitHub: `sab-modules`' `validate` job succeeded in 16 seconds on the first push, confirming the cross-repo checkout-and-run design actually works, not just in theory. **The real design question:** how does `sab-modules`' CI reach validation logic that lives in a different repo, `sab-engine`? Considered a published NuGet `dotnet tool` (cleanest long-term, matches the Community Contribution Framework's eventual vision — but real setup cost: publishing infrastructure, an API key secret, a versioning strategy, premature while Phase 1's schema is still actively changing) versus duplicating a lightweight validator directly in `sab-modules` (zero cross-repo coupling, but creates exactly the "two places the rules could drift apart" problem PD-12 was built to avoid). **Landed on a middle ground, explicitly a deliberate Phase 1 simplification:** `sab-modules`' CI checks out `sab-hq/sab-engine` (public, no token needed) alongside itself and runs `dotnet run --project sab-engine/src/SabEngine.Modules.Cli -- sab-modules/modules` — single source of truth, zero publishing infrastructure, works today. Revisit as a published tool once the OSML has real external contributors who'd benefit from validating locally without a full `sab-engine` checkout. New `SabEngine.Modules.Cli` project (in `sab-engine`) walks a directory for every `manifest.yaml`, parses each via PD-12's real parser, prints OK/FAIL per file plus a summary, and exits non-zero on any failure — that exit code is what makes it usable as an actual CI gate. |
| PD-14 | Write the `pre-flight-check` module (checks a server is healthy enough to patch) | First real module — the workflow's first step, and the one with the least risk since it only reads | ✅ Done — **verified locally by Brock:** `Invoke-Pester` passes 8/8 (matches the 5 `Context` blocks' `It` counts exactly — an earlier note in this entry said "9 tests," which was a miscount on my part, not evidence of anything skipped). Pester installed as **v6.0.1**, not the v5 the test file's syntax was written against (`Describe`/`Context`/`It`/`BeforeAll`/`BeforeEach`/`Mock`) — flagged as a real compatibility unknown before running; turned out v6 handles v5-style syntax with zero issues. Written in `sab-modules/modules/pre-flight-check/`: `manifest.yaml` (matches PD-12's schema exactly — `id`, `risk_level: low`, `validation_status: lab-validated` per the standard starting point, typed `inputs`/`outputs`, a required `rollback` block, and `tests.lab_suite`), `pre-flight-check.ps1` (real logic: checks free disk space against a configurable threshold, checks for an already-pending reboot via three separate registry indicators, checks the Windows Update service isn't disabled — returns a `Healthy`/`Reasons` object), `rollback-pre-flight-check.ps1` (a deliberate no-op, not an omitted file — see the note on PD-12/Section 4.2's hard rule below), and `pre-flight-check.tests.ps1` (every external check mocked, so the tests verify the script's own logic rather than whatever happens to be true on whichever machine runs them). **A real design decision made along the way:** `modules.md` had already noted read-only modules like this one "don't need a *real* rollback" — but PD-12's parser enforces `rollback.procedure` unconditionally, with no exception. Rather than special-case the parser (already verified, working code), this module just gets a genuine no-op rollback script — keeps the orchestration engine's future "call the module's rollback on failure" logic uniform across every module, no branching on module type. **A real, honest gap this surfaced, closed by PD-13:** there was no way to confirm `manifest.yaml` actually parsed cleanly against `SabEngine.Modules` (PD-12) without either a throwaway cross-repo test or a real validator — written carefully to match the schema (and specifically avoiding the flow-mapping quoting gotcha PD-12 surfaced in the design doc itself), but genuinely unverified against the real parser until PD-13's `SabEngine.Modules.Cli` confirmed it directly (`1/1 manifest(s) valid`). |
| PD-15 | Write the `stage-patches` module | Second step in the patching workflow | ✅ Done — **verified locally by Brock: Pester passes 5/5, and `SabEngine.Modules.Cli` confirms both manifests valid (`2/2 manifest(s) valid`).** `manifest.yaml` matches PD-12's schema (`risk_level: low`, `validation_status: lab-validated`). `stage-patches.ps1` downloads (never installs) available Windows updates via the native Windows Update Agent (WUA) COM API, with two COM-interop boundaries (`New-UpdateSession`, `New-UpdateCollection`) isolated into their own functions so Pester can mock them. `rollback-stage-patches.ps1` is a deliberate no-op, same reasoning as PD-14. **Two real bugs found and fixed during verification, both in the test file, not the module logic:** (1) helper fake-object functions (`New-FakeUpdate`, `New-FakeSession`) defined at the top of the file, outside any Pester block, weren't reliably visible inside `It`/`BeforeEach` scope in Pester v6 — fixed by moving them into `BeforeAll`, the officially-supported mechanism for this. (2) A genuinely interesting, classic PowerShell gotcha: mocking `New-UpdateCollection` to return `[System.Collections.ArrayList]::new()` (an **empty** collection) resulted in `$updateColl` silently ending up `$null` — because `ArrayList` implements `IEnumerable`, and PowerShell auto-unrolls any enumerable flowing through a scriptblock's pipeline output; an *empty* enumerable unrolls to *zero* output items, so the assignment received nothing. Diagnosed via a deliberate debug trail (confirmed the mock was actually being invoked before suspecting the return value itself), fixed with `Write-Output -NoEnumerate`, which explicitly tells PowerShell not to unroll a returned collection. This is exactly the kind of subtle bug that never would have been caught without actually running the tests. **A genuinely new risk category, more novel than anything in `sab-engine` itself:** this is COM interop, not just cmdlets — these tests verify `Invoke-StagePatches`'s own branching logic given a WUA-shaped response, but do **not** prove the real WUA COM API actually behaves the way this script assumes; that can only be verified by running the real script against a real Windows machine with pending updates — not done yet, and not urgent given PD-14's same limitation is already accepted. **A real, important safety distinction from PD-14:** unlike `pre-flight-check`, running `stage-patches.ps1` directly (not through the mocked tests) triggers a genuine Windows Update search/download on whatever machine runs it — do not run the script directly on a local dev machine out of habit. |
| PD-16 | Write the `apply-patches` module | Third step — the highest-risk module, which is why everything above (audit logging, secrets, state machine) needs to exist first | ✅ Done — **verified locally by Brock, clean on the first attempt: `Invoke-Pester` passes 10/10, and `SabEngine.Modules.Cli` confirms all three manifests valid (`3/3 manifest(s) valid`).** `manifest.yaml` matches PD-12's schema, with `risk_level: high` — the first module to earn that rating, since this is the only one in the workflow so far that actually changes a server's running state. `apply-patches.ps1` reuses stage-patches' COM-interop boundary pattern (`New-UpdateSession`, `New-UpdateCollection`) plus `CreateUpdateInstaller()`/`Install()`, and includes a real safety boundary: it will never install an update that isn't already marked `IsDownloaded` — even if a caller's `patch_ids` filter matches it — covered by its own dedicated test. **Unlike PD-14/PD-15, `rollback-apply-patches.ps1` is a genuine, non-trivial rollback, not a no-op** — uses `wusa.exe /uninstall /kb:...` to actually remove specific patches, since the WUA COM API itself doesn't expose an uninstall-by-KB method. Both scripts' logic are tested together in one `apply-patches.tests.ps1`, since the Section 4.2 manifest schema only supports a single `tests.lab_suite` field per module. Applied the `-NoEnumerate` lesson from PD-15 proactively rather than rediscovering it — no repeat of that bug this time. One invalid Pester assertion (`Should-Not`, not real syntax) was caught and removed during self-review, before this ever reached Brock. |
| PD-17 | Write the `validate` module (confirms the server came back up correctly) | Fourth step, confirms PD-16 actually worked | ✅ Done — **verified locally by Brock, clean on the first attempt: `Invoke-Pester` passes 6/6, and `SabEngine.Modules.Cli` confirms all four manifests valid (`4/4 manifest(s) valid`) — all four patching modules now exist, tested, and verified.** `manifest.yaml` matches PD-12's schema (`risk_level: low`, `validation_status: lab-validated`). `validate.ps1` closes the loop `pre-flight-check` opened: confirms each requested `patch_id` actually shows as installed now (via a WUA search with `IsInstalled=1`, reusing the `New-UpdateSession` boundary pattern), plus a post-patch health re-check reusing `pre-flight-check`'s disk-space/WU-service logic — deliberately excluding the pending-reboot check, since a reboot being pending right after `apply-patches` is normal and expected here, not a failure signal. `rollback-validate.ps1` is a deliberate no-op, same reasoning as `pre-flight-check` (PD-14). Tests combine both established mocking patterns from the last two modules (WUA search mocking from PD-15, health-check cmdlet mocking from PD-14) across 6 `Context` blocks, including one that deliberately checks both conditions matter together (patches confirmed but system unhealthy → still `Succeeded: false`). |
| PD-18 | Write tested rollback procedures for `stage-patches` and `apply-patches` (`pre-flight-check` and `validate` don't need one, since they only read) | Required before any of these modules can be marked usable, per Section 2's non-negotiable rollback rule | ✅ Effectively done, via PD-15 and PD-16 — no separate work needed. `stage-patches` got a deliberate, justified no-op rollback (PD-15: downloading doesn't change running behavior, so there's nothing urgent to undo). `apply-patches` got a genuine, non-trivial rollback (PD-16: `wusa.exe /uninstall`, actually reversing an installed patch). Both are already covered by their respective modules' Pester tests — not tracked as separate work here to avoid double-counting the same thing under two IDs. |
| PD-19 | Assign each module (PD-14–PD-17) a unique ID per AR-5/AR-6 (confirmed) and set initial `validation_status: lab-validated` | Can't be skipped — the engine won't recognize a module without one | ✅ Done — built into each module's `manifest.yaml` as it was written (PD-14–PD-17), not deferred as separate work. All four (`pre-flight-check`, `stage-patches`, `apply-patches`, `validate`) have unique IDs and `validation_status: lab-validated`, confirmed by `SabEngine.Modules.Cli` (`4/4 manifest(s) valid`). |
| PD-20 | Write the `lab_suite` test file for each module (per the Section 4.2 metadata schema's `tests` field) | Depends on the modules (PD-14–PD-18) actually existing to write tests against | ✅ Done — written alongside each module, not as separate work. All four modules' `tests.lab_suite` files are real and passing: `pre-flight-check.tests.ps1` (8/8), `stage-patches.tests.ps1` (5/5), `apply-patches.tests.ps1` (10/10), `validate.tests.ps1` (6/6) — 29 tests total across the module set, all verified by Brock. |
| PD-21 | Write the first real workflow definition — "Patch Windows Server" — stringing PD-14–PD-17 together in order (Section 4.2, "Workflows as a separate layer") | Depends on all four modules existing first | ⬜ Not Started |
| PD-22 | Give the workflow its own unique ID per AR-5 (confirmed) | Same requirement as PD-19, applied to the workflow itself | ⬜ Not Started |
| PD-23 | Build the WinRM connector implementing the Section 4.4 interface (`connect`, `execute`, `disconnect`, `health_check`) | Needed before any module can actually run against a real (or lab) target | ⬜ Not Started |
| PD-24 | Implement `credential_handle` resolution against the secrets backend (PD-9) at connection time (never exposing raw credentials to modules or the AI agent) | Depends directly on PD-9 and PD-23 both existing | ⬜ Not Started |
| PD-25 | Scope least-privilege credentials per module/target rather than one standing credential (Section 4.4) | Refines PD-24 once basic credential resolution works | ⬜ Not Started |
| PD-26 | Implement per-target connection isolation (a hang on one target can't affect others running in parallel) | Extends the connector (PD-23) — matters once more than one target is ever run against at once | ⬜ Not Started |
| PD-27 | Implement Docker-based sandboxing for module execution, per SE-2 (confirmed) | Wraps module execution (PD-14–PD-17 running through PD-23) in isolation before real testing begins | ⬜ Not Started |
| PD-30 | Build a human approval surface — something a person actually looks at to approve or decline a pending plan | **A real gap in the original PD-1–PD-29 list, discovered mid-project, not deferred on purpose.** The state machine (PD-4) *enforces* that a human must approve a plan before `Executing`, but nothing existed for a human to actually use to do that approving. Positioned here, before PD-28, since PD-28's exit criteria explicitly requires "a human approving each run" — that's not possible without this. ID assigned as PD-30 (next available, per this doc's own numbering rule) even though it slots in sequentially before PD-28–PD-29. | ✅ Done — **verified locally by Brock: the full Create → Review → Approve/Decline flow works end to end in a real browser.** `dotnet build` clean (0 warnings, 0 errors), `dotnet run --project src/SabEngine.Api` hosts on `http://localhost:5000`. Plain server-rendered HTML — no JavaScript framework, no build step — matching the same lightweight philosophy `sab-kb`'s own UI already uses. Three routes: `GET /` lists runs in `PendingApproval`; `GET /runs/{id}` shows the plan's steps and the agent's reasoning with Approve/Decline buttons; `POST /runs/{id}/decide` actually drives `WorkflowRunStateMachine.TransitionAsync` and records a real `Approval` row (who, which plan, per Section 4.1's data model) — not a mockup. A `POST /demo/create` route seeds an obviously-labeled demo run using the real `pre-flight-check` module ID, since no code yet creates a real plan (the orchestration engine still doesn't call modules in sequence — PD-4's own noted gap). **Two real bugs found and fixed during verification, both flagged as risks beforehand:** (1) `NETSDK1022` — switching to `Microsoft.NET.Sdk.Web` (needed for Kestrel hosting) made the Web SDK auto-include `appsettings.json` as a Content item, duplicating an explicit `<Content Include>` block left over from the old plain-console `Sdk`; removed the now-redundant explicit block. (2) `CS9006` — the CSS blocks used old-style brace-doubling (`{{ }}`) to escape literal braces, which is the wrong escaping convention for C# *interpolated raw string literals* (`$"""..."""`); fixed by pulling CSS into its own plain, non-interpolated raw string constant (no `$` prefix, so braces need zero escaping at all) and referencing it via a single, unambiguous `{HomeCss}`/`{RunCss}` interpolation — cleanly separates "literal braces" from "real C# interpolation" instead of mixing them in one string. |
| PD-28 | Run and pass the Phase 1 exit criteria end-to-end: SAB reliably patches a lab server (PD-11), with a human approving each run and a tested rollback path proven to actually work — not just documented (Section 9, Phase 1) | The integration point — depends on everything above being in place | ⬜ Not Started |
| PD-29 | Promote validated modules (PD-14–PD-17) from `lab-validated` to `production-approved` | Only happens once PD-28 actually passes | ⬜ Not Started |

---

## Explicitly Deferred — Not Blocking Phase 1

These are real open items, but the design doc and `open-questions.md` already establish they don't need to be resolved before development starts. Listed here so they don't get mistaken for missing prerequisites, and so they don't clutter the priority order above.

| Ref | Item | Why it's deferred |
|---|---|---|
| AR-4 | Third-party integration API surface | Phase 3/4 scope, per the roadmap |
| SC-1/SC-2 | Multi-server/concurrent-run scaling detail | Worth a real load-testing pass once Phase 1 exists, not before |
| PR-1/PR-2 | Reliability/uptime and rollback-time targets | Reasonable to set once Phase 1 gives real timing data to anchor against |
| MP-1 | Marketplace revenue share % | Irrelevant until Phase 2's marketplace |
| SE-3 | Compliance framework target (SOC2, HIPAA, etc.) | Blocked on knowing a target industry, not on more design work |

---

### Notes
- This is a living tracking doc, in the same spirit as `open-questions.md` — pulls concrete pre-code tasks out of the design doc and roadmap so they're trackable in one place, in the order they actually need to happen.
- IDs are permanent once assigned — if priorities shift and the order changes, move the row and note why, rather than renumbering everything.
- Update status as items move. When an item is genuinely done, leave it marked ✅ rather than deleting it — the history of what's been completed is useful context on its own.
- New items surfacing during development belong here too, inserted at the point in the sequence where they actually apply, with a new PD-N ID.
