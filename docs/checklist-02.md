# Checklist 02

### `sab-engine` — Active Tracker

**Status: ACTIVE.** This is now the single source of truth for what's currently in progress or not yet started. `pre-development-checklist.md` is closed — a historical record of PD-1 through PD-27 and PD-30, all done and verified. This document picks up where that one left off: PD-28 and PD-29 (moved here unstarted), the items that were explicitly deferred there, and every open thread flagged along the way (the untested WinRM network path, `CredentialHandleResolver`'s per-module wiring, Docker sandboxing's Windows-container gap).

**Ordering:** Items are listed in the order they actually need to happen — each one generally depends on the ones above it. IDs are assigned once and don't get renumbered as items complete; if the order needs to change later, move the row and add a note rather than reassigning IDs.

**Status key:**
- ⬜ **Not Started**
- 🟡 **In Progress**
- 🔒 **Blocked** — waiting on something else, noted in the item
- ✅ **Done**

---

| ID | Item | Why it's here / depends on | Status |
|---|---|---|---|
| PD-35 | Decide whether an orphaned-run health re-check should literally invoke the `validate` module, or a lighter-weight variant | **A real prerequisite for PD-33, not a follow-up from it — moved ahead of PD-33 for that reason.** Split out because it's a genuinely separate decision with its own complications. | ⬜ Not Started (design decided, no code yet) — **settled, reasoned from how real SAs/SEs actually work, not just a preference:** **(1) Reuse `validate` (PD-17) directly — no lighter-weight variant.** A real SA doesn't maintain two versions of "is this server okay" logic, one normal and one trimmed-down for recovery specifically — that's more surface area to maintain, and every place the two diverge is a place the shortcut could miss something the full check would catch. One trusted checklist, run whenever ground truth is needed. **(2) Sequenced as two tiers, matching how an SA actually reconnects to an interrupted box:** first, reachability alone — reuse the already-built, already-tested `WinRmExecutionConnector.HealthCheckAsync` (PD-23), a cheap TCP probe, no assumptions about what was happening when the interruption occurred; only if reachable, run `validate`'s real logic (general health plus specific patch confirmation). This is a sequencing decision, not new code — both pieces already exist. **(3) Which patches to check for is pulled from the `Plan` itself, not new bookkeeping.** The original `apply-patches` step's recorded parameters already contain the `patch_ids` that were part of this change — the engine just reads that, the same way a real SA checks their own change record instead of guessing what they were working on. **(4) This also resolves "which step did it crash during" cleanly:** no recorded `ExecutionResult` for `apply-patches` yet → nothing risky was attempted → just resume from the next step, no health check needed (an SA wouldn't re-verify a server they never touched). An `ExecutionResult` for `apply-patches` exists, complete or not → run the two-tier check above before deciding what's next. |
| PD-37 | Extend `pre-flight-check` (PD-14) to snapshot which KBs are already installed at the **start** of a run, so rollback scoping (PD-36) can distinguish this run's own changes from pre-existing state | **A real prerequisite for PD-36's rollback-scoping formula to actually be correct — surfaced while reasoning through PD-36, not invented independently.** Positioned before PD-36 for that reason, same pattern as moving PD-35/36 ahead of PD-33. | ⬜ Not Started — **the gap, and why it matters:** a meticulous SA never undoes state they didn't create. If a KB was already installed *before* this run ever started — unrelated history — and it happens to also appear in this run's planned patch list, blindly rolling it back on failure would be a real mistake: undoing something this run never actually touched. Without a "before" snapshot, the engine has no way to tell "this run's work" apart from "state that predates it." **What's needed:** `pre-flight-check.ps1` currently checks disk space, pending reboot, and WU service status only — no installed-KB enumeration at all. Add a WUA search on `IsInstalled=1` (the same query shape `validate`, PD-17, already uses for confirmation — just run *before* any changes instead of after) and record the resulting set of already-installed KB IDs as a new output field, likely `InstalledPatchIds` alongside the existing `Healthy`/`Reasons`. **A real, reasonable scoping decision to make while building this:** the snapshot likely only needs to check the specific `patch_ids` in *this run's own plan* (per PD-35's same reasoning — read the `Plan`'s own recorded parameters rather than guessing), not every installed KB on the machine — cheaper, and sufficient for the set-subtraction PD-36 needs. **Where this needs to end up available later:** the orchestration engine needs to read this snapshot back at rollback-decision time, most naturally from `pre-flight-check`'s own persisted `ExecutionResult` for that run — ties into PD-36's broader question about whether `ExecutionResult.Output` needs to become structured data rather than a flattened string. |
| PD-36 | Design how a triggered rollback gets scoped to what's actually broken, not "everything since the start" — given `rollback-apply-patches.ps1` (PD-16) takes an explicit `PatchIds` list rather than inferring what needs undoing on its own | **A real prerequisite for PD-33, not a follow-up from it — moved ahead of PD-33 for that reason.** Depends on **PD-37** for the "before" snapshot the scoping formula below actually needs. | ⬜ Not Started — **design updated after working through PD-37 — the actual scoping formula, reasoned from real SA practice:** don't trust `apply-patches`' own self-reported `Applied` array for this — a real SA doesn't scope a rollback based on what an installer *said* it did, they check what the target actually shows. Re-verify directly against the server instead, reusing the exact same WUA-query approach PD-35 already settled on. **The full formula:** rollback set = (patches this run planned to apply) ∩ (currently installed, per direct re-verification) − (already installed *before* this run started, per PD-37's snapshot). The first two pieces are already solvable with PD-35's decisions; the third is what PD-37 exists to provide. **This changes what used to be framed as the primary blocker:** structuring `WinRmExecutionSession.ExecuteAsync`'s (PD-23) flattened string output is **no longer required just to make rollback scoping work**, since the scoping decision is made by directly re-querying the target's real state, not by parsing `apply-patches`' self-reported result. **Still worth doing eventually, just not a blocker here:** preserving structured output from `ExecuteAsync` remains genuinely valuable for audit trail richness and debugging (seeing exactly what a module reported, not just a flattened log), even though it's no longer on the critical path for correct rollback scoping specifically. |
| PD-33 | Design crash recovery for workflow execution the way a real systems engineer would actually handle it: on finding an interrupted run, **reconnect and assess the target's actual current state before deciding anything** — not a reflexive rollback, not a blind resume | **Depends on PD-35 and PD-36 being resolved first** — this item's design isn't actually complete until both of its concrete sub-decisions (how the health re-check works, how rollback gets scoped) are settled; it's the umbrella principle, not something that can close on its own. Also shapes PD-32's design directly. SAB models real sysadmin practice throughout (recommend-and-approve mode, per-module rollback, health checks before *and* after patching); crash recovery should follow the same principle, not invent its own abstract logic. A controller crash and a target-server problem are different things, and treating the first as automatic grounds to act on the second is a real anti-pattern worth avoiding deliberately. | ⬜ Not Started — **the actual design, reasoned through, not just an open question:** (1) **Detect the orphaned run.** `WorkflowRunClaimService` (PD-5) currently claims `Requested`, `Declined`, `Approved`, `Failed` — **`Executing` is not in that list**, so a run whose worker died mid-execution currently has no path back to being reclaimed at all. This is a real, concrete gap PD-32 needs to close, likely a lease-expiry check on `Executing` mirroring the pattern PD-5 already uses for its other claimable states. (2) **Re-run a health check against the target before deciding anything** — essentially re-invoking `validate`-shaped logic (PD-17) as the very first thing that happens on reclaiming an orphaned run, mirroring exactly what a real engineer would do: reconnect, check what's actually true, *then* decide — the specifics of exactly which check and how are PD-35's job. (3) **Branch on that result, not on the mere fact of interruption:** target healthy → continue/retry the remaining steps (the modules are already idempotent enough for this to be safe — see the WUA `IsInstalled=0` reasoning in PD-35/36); target unhealthy → trigger rollback, scoped to whatever's actually broken (PD-36's job), not a blanket "undo everything since the start." **The one genuinely irreducible risk:** a crash happening mid-install, inside a single WUA operation — an inherent Windows Update risk that exists with or without SAB. |
| PD-32 | Wire the `OrchestrationEngine` to actually execute a workflow: read a workflow definition, call each module in order via `IExecutionConnector`, transition the `WorkflowRun` through `Executing → Completed/Failed` as it goes, and trigger a module's own rollback automatically on failure | **The single foundational gap PD-28 and PD-31 both depend on** — flagged repeatedly since PD-4, never itemized on its own until now. Placeholder entry, written with as much real detail as could be reasoned out now, so whoever picks this up isn't starting from nothing. | ⬜ Not Started — **not yet designed in code, but here's what's already known:** **What it can build on, all real and already verified:** `WorkflowRunClaimService.TryClaimNextAsync` (PD-5) already claims eligible runs atomically, including the `Approved` state — this is the natural trigger point; a background worker (likely an ASP.NET Core `BackgroundService` hosted in `SabEngine.Api`, since that's already the live host per PD-30) would poll it, and claiming an `Approved` run is what kicks this logic off. `WinRmExecutionConnector`/`WinRmExecutionSession` (PD-23–PD-27) already resolve a target, execute a module script, and return a structured `ExecutionResult` — the actual step-execution primitive already exists and is tested. `WorkflowRunStateMachine` (PD-4) already enforces the transitions this engine would drive. **Three real, unresolved sub-problems worth flagging honestly, not glossed over:** (1) **No workflow parser exists.** `patch-windows-server.yaml` (PD-21) has zero automated reader — this item will likely need to build a minimal one (deserialize the YAML into an ordered list of `module_id`/`module_version` steps) as part of its own scope, since there's nothing to execute against otherwise. (2) **Rollback script resolution is a real, concrete gap in already-built code.** `WinRmExecutionSession.ExecuteAsync` currently resolves `moduleId` → `{modulesRoot}/{moduleId}/{moduleId}.ps1` only — it has no way to invoke a module's *rollback* script, which lives under a different filename (e.g. `rollback-pre-flight-check.ps1`, per each manifest's `RollbackSpec.Procedure`, PD-12). This will need either a new parameter/overload on `ExecuteAsync`/`IExecutionSession`, or another mechanism — genuinely undecided. (3) **Scoping call on `CredentialHandleResolver` (PD-31):** this item should NOT hard-block on PD-31 — it's reasonable to build PD-32 using one connection/credential per workflow run first, and layer in PD-31's per-module tiered credentials as a refinement afterward, matching how PD-25 itself was scoped (the mechanism before the wiring). **See PD-33, PD-35, and PD-36 for related, already-reasoned-through design questions that directly shape this item** — crash recovery, the health-recheck approach, and (importantly) a likely need to change `ExecutionResult.Output` from a plain string to something structured. **Also still needed, not blocking but relevant:** persisting an `ExecutionResult` row per step (the type already exists in Core, matching Section 4.1's data model) as the engine goes, not just at the end. |
| PD-34 | Clarify: Docker sandboxing (PD-27) is **not** part of the real WinRM execution path PD-32 will build | Prevents a real, easy-to-make mistake — conflating two separately-built things that sound related but aren't | ✅ Done — **already resolved; recorded here so it doesn't get confused later, not left as an open question.** `DockerSandboxedExecutor` (PD-27) runs a script *locally*, on the machine running `sab-engine` — it was built as a standalone validation/dry-run tool (e.g. for safely test-running a module's logic before trusting it, or as part of a future module-promotion step). `WinRmExecutionConnector`/`WinRmExecutionSession` (PD-23–PD-27) execute a script *remotely*, on the actual target server being patched — that's the real execution path PD-32 wires up. These don't compose: Docker sandboxing is local, WinRM execution is remote and on a completely different machine. PD-32 should not attempt to route real module execution through `DockerSandboxedExecutor` — they're two separate tools solving two separate problems, not layers of the same pipeline. |
| PD-28 | Run and pass the Phase 1 exit criteria end-to-end: SAB reliably patches a lab server (PD-11), with a human approving each run and a tested rollback path proven to actually work — not just documented (Section 9, Phase 1) | Directly depends on **PD-32** (nothing to run end-to-end without it) and on the WinRM connector's real network path being verified against the lab VM (never tested — only RDP was, not WinRM/5985) | ⬜ Not Started |
| PD-29 | Promote validated modules (PD-14–PD-17) from `lab-validated` to `production-approved` | Only happens once PD-28 actually passes | ⬜ Not Started |
| PD-31 | Wire `CredentialHandleResolver` (PD-25) into per-module execution — each module in a workflow resolves its own appropriately-scoped credential and opens its own connection, instead of one connection/credential covering an entire workflow run | Depends on **PD-32** existing first (there's no per-module execution to wire tiered credentials into yet). `CredentialHandleResolver` itself is real and tested (PD-25); nothing calls it per-module yet. Deliberately not a hard blocker for PD-28 — see PD-32's own scoping note. | ⬜ Not Started |

---

## Explicitly Deferred — Not Blocking Phase 1

These are real open items, but the design doc and `open-questions.md` already establish they don't need to be resolved before development starts. Listed here so they don't get mistaken for missing prerequisites, and so they don't clutter the priority order above.

| Ref | Item | Why it's deferred |
|---|---|---|
| AR-4 | Third-party integration API surface | Phase 3/4 scope, per the roadmap |
| SC-1/SC-2 | Multi-server/concurrent-run scaling detail | Worth a real load-testing pass once Phase 1 exists, not before |
| PR-1/PR-2 | Reliability/uptime and rollback-time targets | Reasonable to set once Phase 1 gives real timing data to anchor against |
| MP-1 | Marketplace revenue share % | Irrelevant until Phase 2's marketplace |
| SE-3 | Compliance framework target (SOC2, HIPAA, etc.) | Blocked on knowing a target industry, not on more design work |

---

## Other Known Open Threads (not yet given their own PD- entry)

Flagged honestly across `pre-development-checklist.md`'s entries but not yet formalized as their own tracked item:

- **The real WinRM network connection is untested.** PD-23's connector code is done and verified against a substituted local session, but nobody has yet started the lab VM and confirmed it actually connects over the network (port 5985) to a real target. Worth doing deliberately, likely as part of or just before PD-28.
- **Docker sandboxing doesn't cover SAB's real modules yet.** `DockerSandboxedExecutor` (PD-27) is real and correctly isolates any PowerShell script, but the actual four patching modules are Windows-specific and won't run inside its default Linux container. Actually sandboxing them needs a Windows container image — a deliberate, disruptive Docker Desktop reconfiguration.
- **No real module catalog loader exists.** Flagged since PD-6 — the AI agent's available-module list is still supplied manually rather than loaded automatically from an OSML checkout.
- **No workflow parser/validator exists**, unlike modules (PD-12/13) — though PD-32 above will likely need to build at least a minimal reader as part of its own scope.
- **Wiring the AI agent to a real model** — still needs a real OpenAI/Azure OpenAI API key from Brock, deliberately not hardcoded into the repo. **Open question, not yet resolved:** does PD-28's exit test require the agent to genuinely propose the plan via a real model, or can a fixed/manual plan stand in for that specific test? Worth deciding before PD-28 is attempted, not during it.

---

### Notes
- This doc continues `pre-development-checklist.md`'s numbering and format — PD-28/PD-29 moved here with their IDs unchanged; PD-31 was already here. New genuinely-new items continue from PD-32 onward.
- `pre-development-checklist.md` is now closed — a historical record of PD-1 through PD-27 and PD-30. Nothing further gets added there.
- IDs are permanent once assigned, same rule as the original checklist — if priorities shift and the order changes, move the row and note why, rather than renumbering everything.
- Update status as items move. When an item is genuinely done, leave it marked ✅ rather than deleting it — the history of what's been completed is useful context on its own.
